mysqld:
  image: simpledrupalcloud/mysqld:5.5.38-dev
  hostname: mysqld
  ports:
    - "3306:3306"
  volumes_from:
    - mysqlddata
mysqlddata:
  image: simpledrupalcloud/data:dev
  hostname: mysqlddata
  volumes:
    - ./.docker/mysqld-5.5.38/data:/mysqld-5.5.38/data

