mysqld:
  image: viljaste/mysqld:dev
  hostname: mysqld
  ports:
    - "3306:3306"
  volumes_from:
    - mysqlddata
  environment:
    - PASSWORD=root
mysqlddata:
  image: viljaste/data:dev
  hostname: mysqlddata
  volumes:
    - /mysqld
