mysqld:
  image: simpledrupalcloud/mysqld:dev
  hostname: mysqld
  ports:
    - "3306:3306"
  volumes_from:
    - mysqlddata
  environment:
    - PASSWORD="root"
mysqlddata:
  image: simpledrupalcloud/data:dev
  hostname: mysqlddata
  volumes:
    - /mysqld/data
