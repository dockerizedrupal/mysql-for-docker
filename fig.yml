mysqld:
  image: simpledrupalcloud/mysqld:latest
  hostname: mysqld
  ports:
    - "3306:3306"
  volumes_from:
    - mysqlddata
  environment:
    - PASSWORD=root
mysqlddata:
  image: simpledrupalcloud/data:latest
  hostname: mysqlddata
  volumes:
    - /mysqld
